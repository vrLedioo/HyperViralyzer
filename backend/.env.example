# --- Core ---
# Server-side OpenAI key. Used for credit / subscription / pay-per-use analyses.
# (Users on the BYOK path supply their own key in the UI instead.)
OPENAI_API_KEY=your_openai_api_key_here

# SQLite by default; swap for a Postgres URL in production.
DATABASE_URL=sqlite:///./video_analyzer.db

# --- Auth ---
# Use a long random string (>= 32 chars). Generate one, e.g.:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-me-to-a-long-random-secret-string
FREE_CREDITS_ON_SIGNUP=3

# --- Stripe (test mode) ---
STRIPE_SECRET_KEY=sk_test_...
# From `stripe listen` when forwarding webhooks locally (whsec_...).
STRIPE_WEBHOOK_SECRET=whsec_...
# Recurring price id for the Pro subscription (price_...).
STRIPE_SUBSCRIPTION_PRICE_ID=price_...
PAY_PER_USE_AMOUNT_CENTS=99

# --- URLs ---
FRONTEND_URL=http://localhost:3000

# --- Uploads ---
UPLOAD_DIR=./uploads
MAX_UPLOAD_MB=200
